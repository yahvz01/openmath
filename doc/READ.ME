해당 모듈은 최대 65,556비트 8Byte 8KiB 바이트의 숫자만을 지원한다.

해당 모듈의 데이터 타입은 정수형과 UnLimitedInts

Q. Why don't you use inheritance when creating a class?

Q. Are the classes thread-safe?

What you want to say before using this module


Things getting returned by a pointer is easy to be invalidated.

These Module is not thread-safe.
